Good Morninggirl girl

import express from 'express'

const app = express();

app.get('/' , (req , res) => {

  // console.log(req.query);

  const {number} = req.query;
  console.log(number);

  return res.status(200).json({
    message: "Hello World"
  });

});

app.listen(5000, () => {
  console.log("server is running on port 5000")
})

// enum is a fixed set of value . diyeko vanda aru lidaina